spring:
  application:
    name : restexam

  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 4MB
  datasource:
    url: jdbc:mysql://localhost:3307/tekitexam
    username: rjsgud1956
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
        ddl-auto: update
    show-sql: true

server:
  port: 80